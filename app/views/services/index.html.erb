<h1>Servicios</h1>


  <div id="map"
     style="width: 100%; height: 600px;"
     data-services="<%= @services.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>

<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://res.cloudinary.com/bad-kuky-0ne/image/upload/v1631634844/gatoooooo_efwyd6.jpg);">
  <div class="container">
    <h1>Busca los mejores <strong>servicios de mascotas 🦕🐠🐿🐇🦔</strong></h1>
    <p>Al alcance de tu zona.</p>
    <a class="btn btn-flat" href="#">Contáctanos</a>
  </div>
</div>
<div class="container my-3">
<div class="cards">
  <% @services.each do |service| %>
  <div class ="cards">
      <p><%= service.address %></p>
      <p><%= service.name %></p>
      <p><%= service.city %></p>
      <p><%= service.availability %></p>
      <%= link_to "ver detalles", service_path(service), class:'btn btn-primary'%>
    </div>
  <% end %>

  <%= link_to new_service_path do %>
  <i class="fa fa-plus"></i> <h1> Add a new service </h1>


  </div>
 <% end %>
 